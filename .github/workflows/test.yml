name: Test - Terraform test action
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - '*'         # matches every branch that doesn't contain a '/'
      - '*/*'       # matches every branch containing a single '/'
      - '**'        # matches every branch
    paths:

      - 'src/*'
      - 'action.yml'
      - '.github/workflows/test.yml'

jobs:
  test:
    name: Test - terraform test action
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - uses: actions/checkout@v3
      - name: Prepare Git User
        run: |
            # use github token transparently for cloning private terraform module repositories
            git config --global --add http."https://github.com/Ontracon/".extraheader "Authorization: basic $(echo -n "x-access-token:${{ secrets.GITHUB_TOKEN }}" | base64)"
        shell: bash

      # - id: root_test
      #   name: Test - Root Module
      #   uses: ./
      #   with:
      #     ROOT_DIR: '${{ github.workspace }}/tests/root'
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #     ROOT_MODULE: 'true'
      #     PUBLISH_NAME: 'Results - root'

      - id: aws_test
        name: Test - AWS
        uses: ./
        with:
          ROOT_DIR: '${{ github.workspace }}/tests/aws'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          AUTO_CREATE_TF_BACKEND: 'false'
      
      - id: azure_test
        name: Test - Azure
        uses: ./
        with:
          ROOT_DIR: '${{ github.workspace }}/tests/azure'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          AUTO_CREATE_TF_BACKEND: 'false'
       

  
          
